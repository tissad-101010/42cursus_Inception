# **************************************************************************** #
#                                                                              #
#                                                         :::      ::::::::    #
#    vsftpd.conf                                         :+:      :+:    :+:    #
#                                                     +:+ +:+         +:+      #
#    By: tissad <tissad@student.42.fr>              +#+  +:+       +#+         #
#                                                 +#+#+#+#+#+   +#+            #
#    Created: 2025/01/23 20:42:45 by tissad            #+#    #+#              #
#    Updated: 2025/01/24 16:52:10 by tissad           ###   ########.fr        #
#                                                                              #
# **************************************************************************** #
#                                VSFTPD CONFIGURATION                           #
# **************************************************************************** # 

# Example config file /etc/vsftpd.conf
#
# The default compiled in settings are fairly paranoid. This sample file
# loosens things up a bit, to make the ftp daemon more usable.
# Please see vsftpd.conf.5 for all compiled in defaults.
#
# READ THIS: This example file is NOT an exhaustive list of vsftpd options.
# Please read the vsftpd.conf.5 manual page to get a full idea of vsftpd's
# capabilities.
#
# Allow anonymous FTP? (Beware - allowed by default if you comment this out).
anonymous_enable=NO

#
# Uncomment this to allow local users to log in.
local_enable=YES

#
# Uncomment this to enable any form of FTP write command.
write_enable=YES
allow_writeable_chroot=YES

# Default umask for local users is 077. You may wish to change this to 022,
# if your users expect that (022 is used by most other ftpd's)
local_umask=022
#
# Uncomment this if you want the anonymous FTP user to be able to create
# new directories.
#anon_mkdir_write_enable=YES
#
# Activate directory messages - messages given to remote users when they
# go into a certain directory.
dirmessage_enable=YES

#
# You may override where the log file goes if you like. The default is shown
# below.
xferlog_enable=YES
log_ftp_protocol=YES
xferlog_file=/var/log/vsftpd.log

# Make sure PORT transfer connections originate from port 20 (ftp-data).
connect_from_port_20=YES

# Définir le port FTP (ajusté à 5555 si nécessaire)
listen_port=21

# Exécuter vsftpd en mode standalone (écoute sur IPv4)
listen=NO

# Désactiver l'écoute sur IPv6 pour éviter les conflits
listen_ipv6=YES

# Restreindre les utilisateurs à leur répertoire personnel
chroot_local_user=YES
allow_writeable_chroot=YES

# Activer une liste d'exclusion pour certains utilisateurs du chroot si nécessaire
chroot_list_enable=YES
chroot_list_file=/etc/vsftpd.chroot_list

# Activer l'upload des fichiers pour les utilisateurs anonymes si nécessaire
# (désactivé par défaut pour des raisons de sécurité)
#anon_upload_enable=YES

# Activer la création de dossiers pour les utilisateurs anonymes si nécessaire
# (désactivé par défaut pour des raisons de sécurité)
#anon_mkdir_write_enable=YES

# Activer le format standard des logs
xferlog_std_format=YES

# Définir un message de bienvenue
ftpd_banner=Welcome to the FTP server 🌐📁

# Timeout de session inactif
idle_session_timeout=600

# Timeout de connexion des données
data_connection_timeout=120

#desactive le sendbox sur alpine
seccomp_sandbox=NO

pasv_enable=YES
pasv_min_port=40000
pasv_max_port=50000
